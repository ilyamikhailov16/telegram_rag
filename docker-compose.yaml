services:
  db:
    image: postgres:16
    container_name: postgres
    environment:
      POSTGRES_DB: ${DATABASE}
      POSTGRES_USER: ${USERNAME}
      POSTGRES_PASSWORD: ${PASSWORD}
      
  app:
    build: .
    container_name: app
    depends_on:
      - db
    env_file:
      - .env